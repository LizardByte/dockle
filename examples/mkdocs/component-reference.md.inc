## MkDocs-specific components

### Native admonition

The standard MkDocs admonition extension remains available when a custom title
is useful.

#### Source

```markdown
!!! important "Adapter detail"
    Dockle generates `mkdocs.yml`; the consumer does not maintain it.
```

#### Result

!!! important "Adapter detail"
    Dockle generates `mkdocs.yml`; the consumer does not maintain it.
