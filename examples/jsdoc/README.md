# JSDoc preview

The JavaScript fixture shows how Dockle can own `jsdoc.json` while a project keeps only its documented source. The
generated pages receive the same design tokens used by the Sphinx, Doxygen, MkDocs, and rustdoc outputs.
